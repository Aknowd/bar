{{- $updates := slice -}}

{{- range (where .Site.RegularPages "Section" "updates") -}}
  {{- $item := dict
      "date" (.Date.Format "0102")
      "title" .Title
      "link" .Params.link
      "count" .Params.count
  -}}
  {{- $updates = $updates | append $item -}}
{{- end -}}

{{- $updates | jsonify -}}
